// import {} from "../ActionTypes";

const initialState = {
  allPets: [],
  details: {},
  allUsers: [],
};

const RootReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default RootReducer;
