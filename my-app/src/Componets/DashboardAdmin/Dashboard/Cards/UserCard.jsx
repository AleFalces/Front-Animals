import React from "react";
import {Card} from "@chakra-ui/react"

const UserCard = ({ data: { size, img, sex, species, age } }) => {
    return (
      <div >
        
      </div>
    );
  };

export default UserCard